var SandwichMaker = (function(maker) {

  var condimentPrices = {

  };

  // Augment the original object with another method
  maker.addCond = function() {
    //return ???;
  };

  // Return the new, augmented object with the new method on it
  return maker;
})(SandwichMaker || {});